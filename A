def astar(graph, heuristic, start, goal):

    open_list = [(0, start)]
    cost = {start: 0}
    parent = {start: None}

    while open_list:

        open_list.sort()
        current_cost, current = open_list.pop(0)

        if current == goal:
            path = []

            while current:
                path.append(current)
                current = parent[current]

            return path[::-1]

        for neighbour, weight in graph[current]:

            new_cost = cost[current] + weight

            if neighbour not in cost or new_cost < cost[neighbour]:

                cost[neighbour] = new_cost
                priority = new_cost + heuristic[neighbour]

                open_list.append((priority, neighbour))

                parent[neighbour] = current

# User Input
graph = {}
heuristic = {}

n = int(input("Enter number of nodes: "))

for i in range(n):

    node = input("Enter node: ")

    h = int(input(f"Enter heuristic value for {node}: "))
    heuristic[node] = h

    neighbours = int(input(f"Enter number of neighbours for {node}: "))

    graph[node] = []

    for j in range(neighbours):

        neighbour = input("Enter neighbour node: ")
        weight = int(input("Enter edge weight: "))

        graph[node].append((neighbour, weight))

start = input("Enter start node: ")
goal = input("Enter goal node: ")

print("A* Path:", astar(graph, heuristic, start, goal))


Enter number of nodes: 4

Enter node: A
Enter heuristic value for A: 3
Enter number of neighbours for A: 2

Enter neighbour node: B
Enter edge weight: 1

Enter neighbour node: C
Enter edge weight: 3

Enter node: B
Enter heuristic value for B: 2
Enter number of neighbours for B: 1

Enter neighbour node: D
Enter edge weight: 1

Enter node: C
Enter heuristic value for C: 1
Enter number of neighbours for C: 1

Enter neighbour node: D
Enter edge weight: 1

Enter node: D
Enter heuristic value for D: 0
Enter number of neighbours for D: 0

Enter start node: A
Enter goal node: D
